# 🚀 部署说明

## 当前状态

✅ 项目文件已创建
✅ Python 依赖已安装
⚠️  需要配置 MiniMax API Key

## 配置 API Key

编辑 `.env` 文件，将 `your_minimax_api_key_here` 替换为你的真实 API Key：

```bash
# 使用你喜欢的编辑器打开
nano .env
# 或
vim .env
```

修改为：
```
MINIMAX_API_KEY=你的真实API_Key
PORT=8000
```

## 启动服务

配置完成后，运行以下命令之一：

### 方法 1：使用启动脚本（推荐）
```bash
./start.sh
```

### 方法 2：直接运行
```bash
python3 main.py
```

### 方法 3：开发模式（支持热重载）
```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

## 访问应用

启动成功后，在浏览器中访问：

```
http://localhost:8000
```

或者如果是远程服务器：

```
http://你的服务器IP:8000
```

## 测试流程

1. 在首页输入你的心情（例如："今天很开心"）
2. 点击"生成我的专属音乐"
3. 等待 10-30 秒（AI 生成需要时间）
4. 查看生成的歌词和音乐风格
5. 播放音乐
6. 下载或分享

## 获取 MiniMax API Key

如果还没有 API Key：

1. 访问 https://api.minimaxi.com
2. 注册账号
3. 在控制台创建 API Key
4. 确保账户有足够的余额

## 端口占用问题

如果 8000 端口被占用，可以修改 `.env` 文件中的 PORT：

```
PORT=8080  # 或其他可用端口
```

## 后台运行

使用 nohup 或 screen 在后台运行：

```bash
# 使用 nohup
nohup python3 main.py > app.log 2>&1 &

# 或使用 screen
screen -S music-app
python3 main.py
# 按 Ctrl+A 然后按 D 分离会话
```
